# 1.name：工作流名称
name: hello world!
# 2.on：触发该工作的事件
on:
  push:
    branches:
      - main
jobs:
  hello_world_job:
    runs-on: ubuntu-22.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"